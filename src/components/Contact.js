import React from 'react';

const Contact = (props)=>{
	return(
		<div className="container">
			<h1 className="pageTitle">Contact</h1>
			<p>Random Text Contact</p>
		</div>
	)
}

export default Contact;